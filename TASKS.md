# TASKS — 優先順位付きバックログ

## P0 — 今週末までに必須

### P0-1 茅野・諏訪周辺の候補を抽出 [完了]

目的:
長野県Lv0から直近エリアの候補を切り出す。

作業:

- 既存CSVの取得元と行政資料を使って候補を特定
- 必要なら原村・富士見町まで拡張
- `data/processed/suwa_chino_candidates.csv` を作る

完了条件:

- 実在確認済み
- IDを保持
- 10〜30件程度

現在地:

- `data/processed/suwa_chino_candidates.csv` を17件で作成済み

### P0-2 候補に地図表示用の位置を付与 [完了]

目的:
MVP地図に載せる。

最小データ:

- ID
- 林道名
- 表示緯度
- 表示経度
- 入口緯度
- 入口経度
- 位置情報の取得元

最初は路線全体のLineStringがなくてもよい。入口または代表点のピンで開始可能。

完了条件:

- 10件以上を地図表示できる
- 位置の出典が残っている

現在地:

- `data/processed/suwa_chino_locations.csv` に10件作成済み
- 諏訪市7件は公式PDF概要図、茅野市3件はOSM代表点

### P0-3 実走候補3〜5本を選定 [完了]

選定基準:

- 今回の行動エリア
- 未舗装の可能性を示す公開情報
- アクセス現実性
- 情報量

注意:
Lv0と異なり、この段階から未舗装・通行情報の調査を行う。

現在地:

- 本命4本を選定済み: 猿ヶ入 / 棚嵐線 / 赤ジッコ線 / 扇平南峠線
- 予備1本: 付上線
- 根拠ファイル: `data/processed/ride_shortlist_2026-07-08.json`

### P0-4 3〜5本のカルテ作成 [完了]

最小カルテ:

- ID
- 林道名
- 一言要約
- 入口
- 出口または終点
- 未舗装状況
- 通行情報
- 主な注意点
- 最終確認日
- 情報信頼度
- 情報源

不明は不明とする。推測で埋めない。

現在地:

- `data/processed/karte.json` に4本分を作成済み
- `app/` でも正式カルテ内容を優先表示するよう反映済み

### P0-5 MVP地図を実装 [完了]

既定仕様:

- 静的Web
- モバイルファースト
- Leaflet
- ピンまたは線
- タップでカルテカード
- 「Google Mapsで入口までナビ」ボタン
- 候補だけ表示するフィルター

現在地:

- `app/` にLeaflet静的MVPを実装済み
- 候補10件を表示し、タップでカード表示、Google Mapsナビ遷移リンクあり
- 本命 / 予備 / 全件の表示切替あり
- 選択中の候補をURL共有できる
- 公開URL化まで完了

### P0-6 公開・スマホ実機確認 [進行中]

- 共有URLを作る
- Android/iPhoneサイズで確認
- ナビ遷移確認
- 仲間がURLだけで開けることを確認

現在地:

- 公開URL: `https://tetsu19950315-web.github.io/japanese-rindo-database/`
- 共有URL形式 `?view=selected&road=NGN-000010` の HTTP 200 応答確認済み
- 公開カルテ JSON の HTTP 200 応答確認済み
- 残りは実機ブラウザ確認と Google Maps 実遷移確認

### P0-7 現在地・入口距離 [完了]

- 現在地マーカーと精度円を表示
- 選択中の入口までの直線距離を表示
- 位置情報拒否・取得失敗時も既存機能を継続
- 入口座標をワンタップでコピー

### P0-8 現地判断カルテ [完了]

- 通行情報、確認日、信頼度、注意事項をカルテ上部へ集約
- 出典などは折りたたみ、現地で必要な情報を先に表示

### P0-9 PWA・基本オフライン対応 [完了]

- ホーム画面追加用マニフェストとアイコンを追加
- アプリ本体、カルテ、候補、路線データをオフライン保存
- 閲覧済み地図タイルを実行時キャッシュ
- 完全オフライン地図は引き続き非目標

### P0-10 林道路線表示 [完了]

- OSM根拠がある8線形を `routes.geojson` へ収録
- 猿ヶ入の路線名一致線形を実線表示
- 公式図代表点付近の7線形は本線未確定の参考破線として表示

### P0-11 端末内現地記録 [完了]

- 通行状況、路面、ゲート、確認日時、メモ、現在地を保存
- 写真を縮小して最大3枚まで保存
- 再読込後も端末内に保持
- 写真込みJSONを書き出し可能

### P0-12 公開版の地図起動修正 [完了]

- 公開版で `L is not defined` を再現し、Leaflet CDNの読み込み失敗を特定
- Leaflet 1.9.4のJavaScript/CSSをアプリ内へ同梱
- Service WorkerとPages公開バンドルへ同梱ファイルを追加
- 起動失敗時の表示を地図機能とデータ読み込みで区別

## P1 — 週末の実走直後

- 実走結果を更新
- 写真紐付け
- GPX追加
- 通行状況更新
- カルテ再生成
- 現地調査項目の見直し

## P2 — 保留

- 県全域Lv0拡充
- OSM一括照合
- GeoJSON路線化の県全域展開
- 検索・高度フィルター
- 完全オフライン地図

## P3 — 将来構想

- Supabase
- Flutter
- ユーザー投稿
- オフライン地図
- 全国版
- AI自動更新
